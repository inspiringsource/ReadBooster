// Public Gemini app-shell evidence confirms chat-window/chat-window-content roots, but authenticated
// virtualization remains manually unverified. The generic finder therefore requires the same
// concrete overflow and shared-candidate evidence as ChatGPT before any scrolling occurs.
export {
  createConversationScanSource as createGeminiConversationScanSource,
  findConversationScroller as findGeminiConversationScroller,
} from "../conversationDomScanner";
